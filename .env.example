# Google OAuth credentials for Gmail access
# Create at: https://console.cloud.google.com > APIs & Services > Credentials
# Enable the Gmail API, create OAuth 2.0 Client ID (Desktop app)
# Set redirect URI: http://localhost:1456/auth/gmail/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
